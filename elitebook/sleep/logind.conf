[Login]
# Suspend on lid close, then hibernate after HoldoffTimeoutSec if still sleeping
HandleLidSwitch=suspend-then-hibernate
HandleLidSwitchExternalPower=suspend-then-hibernate
HoldoffTimeoutSec=30s
