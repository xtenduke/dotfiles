[ids]

*

[main]

# Tap capslock for escape, hold it for the nav layer (hjkl arrows).
# overload(layer, key): hold = activate layer, tap = send key.
capslock = overload(nav, esc)

[nav]

# vim-style arrows
h = left
j = down
k = up
l = right
